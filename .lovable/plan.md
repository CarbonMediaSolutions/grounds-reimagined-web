

## Add Meta (Facebook) Pixel to index.html

Add the Meta Pixel code to `index.html`:

1. **`<script>` block** — Insert after the GTM script in `<head>` (around line 14), before the preload links.
2. **`<noscript>` fallback** — Insert after the existing GTM noscript block in `<body>` (around line 46).

Pixel ID: `1214835900399407`

